import "./LogsTableStatistics.scss";

type Props = {
  statistics: Record<string, string>;
};

function LogsTableStatistics(props: Props) {
  return <div className="statistics">...</div>;
}

export default LogsTableStatistics;
